# TCG-safe libguestfs appliance defaults for Docker conversion workers without KVM.
# Backend/accel are set via LIBGUESTFS_BACKEND* environment variables.
# See libguestfs-tools.conf(5).

[appliance]
cpus=1
memsize=768
